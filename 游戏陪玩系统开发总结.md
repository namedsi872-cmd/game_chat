# 游戏陪玩系统开发总结

## 项目现状

这个项目已经不再是一个“单文件调用大模型”的小实验，而是一个逐步成型的智能体产品原型。它的目标很明确：做一个带角色设定、带记忆、能聊天、能训练、能承接语音输入，并且后续还能继续扩展工作流的游戏陪玩系统。

当前项目的主题，是围绕《第五人格》做角色化陪聊与训练辅助。当前主角色以夜神月、弥海砂这类角色 Prompt 为核心，但代码结构已经不是“只服务一个角色”的写法了，而是朝着“以后还能继续加新角色”的方向在设计。

从学习阶段来看，这个项目最重要的进步，不是多写了几个接口，而是你已经开始从“让模型回答一句话”转向“让系统有用户、有会话、有角色、有历史、有状态流转”。这一步非常关键，因为这说明你已经从“会调用 API”走向“开始搭系统”了。

---

## 当前目录与职责

### `game_chat/` 根目录

这里放的是整个项目的核心业务逻辑，包括角色 Prompt、模型调用、记忆系统、角色加载器、工作流和配置。它更像整个智能体的大脑层，负责“怎么理解输入、怎么组织上下文、怎么决定走哪个模式、怎么回复”。

### `backend/`

这里是 FastAPI 后端。它负责把前端发来的 HTTP 请求接住，再调用你前面写好的业务逻辑，比如登录、切换角色、聊天、创建会话、查看历史消息、语音识别等。它自己不是“智能体”，而是智能体对外暴露能力的接口层。

### `frontend/`

这里是 Vue 3 + TypeScript 前端。它负责页面展示、用户交互、表单输入、会话切换、消息显示、语音按钮、登录弹窗等。前端的职责不是做训练分析，而是把后端能力接出来，让用户能正常使用。

### `workflow/`

这里是你后面引入的图式工作流部分。现在已经有 LangGraph 的最小链路了，至少做到了“先判断意图，再加载记忆，再进入后续模式处理”。它的意义是帮你把原本混在 `/chat` 里的逻辑慢慢拆开，让后续加训练流、绘图流、复盘流时不至于彻底失控。

### `prompts/`

这里存放角色 Prompt。现在已经不再是把角色设定硬写死在主流程里，而是每个角色一个文件，通过加载器统一读取。这个设计非常重要，因为它决定了“加角色”是补配置，还是推翻重写。你现在走的是正确方向。

---

## 当前完整运行链路

现在这套系统的主链路可以概括成这样：

用户先在前端登录。前端调用 `/login`，拿到 `user_id` 后，把它保存到前端状态里。这里的关键认知是：`user_id` 不是后端自动记住的全局变量，而是前端收到以后，自己保管，再在后续请求里继续带给后端。

接着，用户在首页选择角色，然后点击开始陪玩。前端进入聊天页，并把当前角色名传给 `ChatPanel`。聊天页初始化后，会调用 `/change_role`，把 `user_id + role_name` 发给后端。后端去 `chat_sessions` 表里找这个用户在这个角色下最近的一条会话；如果没有，就创建一条新的；如果有，就直接返回已有的 `session_id`。

之后，用户每发一条消息，前端调用 `/chat`，请求体里带上 `user_id`、`role_name`、`session_id`、`message`。后端收到以后，会先校验这个 `session_id` 是否真的属于当前用户，再根据角色名加载对应 Prompt，根据 `session_id` 重建当前会话的短时记忆，根据当前模式和消息内容判断意图，然后调用模型回复。

回复生成后，系统会把用户消息和 AI 回复都写进 `chat_messages` 表；如果短时记忆有溢出，就把被挤出去的旧消息收集起来，满足条件后交给长时记忆管理器做总结，并把总结结果写进 `long_term_memories` 表。

如果用户切换角色，前端再次调用 `/change_role`，系统就会切到该用户在该角色下的会话。这样，你就实现了“同一个用户，不同角色，不同会话上下文”的隔离。

---

## 当前已经实现的核心能力

## 1. 用户登录与前端用户态

你已经实现了基础的注册登录链路。后端有 `/register` 和 `/login`，前端也已经能把登录弹窗做出来，并在登录成功后接住 `user_id` 和用户名。

更重要的是，你现在已经理解了一个很核心的前后端思维：登录接口返回的数据，不会被 FastAPI 自动记住。真正记住它的是前端。也就是说，登录成功以后，前端要自己保存 `userId`，后面再把它继续传给聊天、切角色、建会话这些接口。

这是你从“脚本式思维”转向“产品式思维”的一个关键节点。

## 2. 角色切换与会话绑定

你已经不再把“当前角色”当作一个页面展示变量，而是把它和后端会话系统真正绑定起来了。

现在的正确绑定关系已经比较清晰：

- 一个用户可以有多个角色
- 每个角色下可以有自己的会话
- 切换角色时，需要重新定位这个用户在该角色下的 `session_id`
- 如果以后再切回这个角色，应该回到原来的那条会话，而不是重新开始

这说明你已经开始真正理解“角色切换”不是单纯改个字符串，而是会话上下文切换。

## 3. 聊天消息持久化

`chat_sessions` 和 `chat_messages` 这两张表已经真正发挥作用了。

现在的设计是标准做法：

- `chat_sessions` 记录一段会话本身
- `chat_messages` 记录这段会话里的每一条消息

这意味着“新建会话”和“继续聊天”已经被分开了。你不再是每发一句就建一条会话，而是在已有会话中持续追加消息。这个结构会让你后面做历史列表、消息回显、长时记忆总结、统计分析都更顺。

## 4. 短时记忆与长时记忆雏形

短时记忆这块，你已经实现了按用户和角色去拿内存对象，也已经逐步把“只按角色分”修正成“按用户 + 角色分”。

更关键的是，你已经把短时记忆和数据库消息记录分开理解了：

- 短时记忆负责当前模型对话时的上下文承接
- 数据库负责持久化保存历史聊天

长时记忆方面，你已经做出了第一版完整闭环：

- 当短时记忆超过上限时，不是简单丢弃
- 而是把被挤掉的旧消息收集起来
- 满足条件后交给长时记忆总结器
- 总结结果写入数据库

这一版还不算最终稳定，但方向是对的。它已经不是“纯内存聊天机器人”，而是开始具备长期信息沉淀能力的智能体。

## 5. LangGraph 最小链路已经接入

你已经把“意图判断 + 加载记忆”从一团聊天逻辑中拆出，放进了 LangGraph。

现在 `graph` 的最小流转是：

- `intent_node` 负责判断当前消息该走 `chat`、`training`、`review` 还是 `draw`
- `load_memory_node` 负责按 `user_id + role_name + session_id` 重建当前会话短时记忆
- 如果当前模式是 `training` 或 `review`，再额外把长时记忆摘要拼进历史上下文

这一层虽然还很轻，但意义很大。它说明你后面继续扩展训练流、绘图流、语音流时，已经有了“工作流入口”，不必把所有判断都堆在 `/chat` 里。

## 6. 历史会话列表与历史消息回显

这一块今天推进得很明显。

你现在不仅能创建新会话，还能：

- 根据 `user_id + role_name` 拉取当前角色下的历史会话列表
- 在右侧会话面板中把这些历史会话显示出来
- 点击某一条历史会话后，再根据 `session_id` 去拉那条会话的消息列表
- 把历史消息重新显示到聊天区里，继续聊下去

这说明你的系统现在已经不是“单轮输入框 + 回复框”的 demo 了，而是有了真正聊天产品的雏形。

## 7. 首页、聊天页、登录弹窗、返回逻辑

前端页面也已经不再是单页乱写了。

你现在做到了：

- 首页和聊天页切换
- 没登录时，点击开始陪玩会先弹登录框
- 登录框以居中悬浮窗形式出现，并带遮罩层
- 登录成功后自动关闭弹窗
- 右上角可以显示当前登录状态
- 从聊天页能返回首页，不需要刷新整个应用

这些能力虽然听起来偏 UI，但它们本质上是在训练你“页面状态流转”的思维。这对以后做更复杂的产品很重要。

## 8. 聊天页样式与历史面板拆分

聊天页现在已经不是简单堆元素了，而是开始有结构意识：

- 会话消息区
- 输入区
- 语音按钮
- 当前会话信息区
- 历史会话列表区
- 新建会话按钮

你还把历史会话面板单独拆成了 `SessionHistory.vue`。这个拆分是对的，因为会话列表本来就不应该和聊天输入逻辑全混在一起。以后继续做搜索历史会话、删除会话、重命名会话时，这种拆分会很有价值。

## 9. 本地语音识别链路已经打通

这是今天最亮眼的一块成果之一。

你现在已经实现了一个从前端到后端再回前端的完整语音识别链路：

前端 `VoiceInput.vue` 通过 `navigator.mediaDevices.getUserMedia({ audio: true })` 获取麦克风音频流，再用 `MediaRecorder` 在“按住说话”期间收集音频块。松开按钮时，把这些音频块拼成一个 `Blob`，再用 `FormData` 形式上传给后端 `/voice/asr`。

后端 `/voice/asr` 接口收到音频文件以后，不是直接把二进制丢给模型，而是先写入临时文件，再调用 `voice_service.py` 中的 `transcribe_audio()`。那里用的是本地 `faster-whisper` 模型，当前是 `WhisperModel("small", device="cpu", compute_type="int8")`。识别得到的文本会通过 JSON 返回给前端。

前端收到 `{ "text": "..." }` 以后，再通过组件事件把文字回填到聊天输入框。也就是说，现在语音输入已经不是“假按钮”，而是真能把一段音频变成一段可发送文本。

## 10. Electron 小窗雏形已经能跑

你已经把前端页面装进 Electron 里了，而且不是普通窗口，而是偏悬浮小窗的方向。

当前入口在 `frontend/electron/main.cjs`，已经完成了这些关键设置：

- 固定窗口宽高
- `alwaysOnTop: true`
- 自动隐藏菜单栏
- 加载本地 Vite 地址 `http://localhost:5173`

这意味着你后面要做“边打游戏边小窗聊天”的体验，已经不是空想了，至少原型层已经能启动。

## 11. 角色 Prompt 已经开始从“中二表演”转向“自然交流”

这一块不是 UI，也不是后端，而是产品体验本身。

你已经发现一个很真实的问题：如果角色 Prompt 写得太“演”，模型就会变得很像在念台词，很假，很不贴近用户当前这句话真正想要什么。

所以现在夜神月的 Prompt 已经不再强调“必须一直端着角色腔调”，而是加入了更重要的规则：

- 先理解用户这句话的社交意图
- 普通聊天默认短答
- 用户只是确认记忆、确认身份、表达情绪时，不要突然切训练模式
- 如果说得奇怪，就收住表演感，改成自然说话

这是非常重要的产品优化。因为一个角色化智能体，不是越像舞台剧越好，而是越能让人愿意继续聊越好。

---

## 2026-07-20 今日新增与推进

今天这一轮推进，最核心的主题其实有三个：聊天页继续产品化、语音输入正式打通、小窗方向开始验证。

第一，聊天页不再只是“发消息”。你已经把它做成了一个真正的聊天工作区：左边是聊天记录和输入区，右边是当前会话信息、历史会话列表和新建会话按钮。更重要的是，历史会话已经不是摆设，而是真的能点击后加载对应消息。

第二，语音输入终于从想法变成可运行链路了。你已经不只是做了一个按钮，而是完整接通了麦克风采集、音频上传、后端 ASR、本地 Whisper 识别、前端回填输入框这整条链。这一步的意义非常大，因为它让你的“陪玩系统”开始向真实使用场景靠近了。边玩边打字很累，但如果能按住说话，使用门槛就会下降很多。

第三，Electron 小窗原型能跑通，证明“桌面悬浮陪聊”的方向不是伪需求。现在它还只是最早期壳子，但至少已经验证了这个产品不是只能在浏览器里用。

第四，体验问题也开始被认真修了。你已经发现模型不该一上来就长篇大论，也不该总是被旧记忆牵着走，更不该用户问一句“你记得我吗”就自动进入训练模式。于是你开始从 Prompt、意图判断、记忆加载范围这几个层面一起调整。这是很成熟的思路，因为体验问题往往不是某一行代码错了，而是多层逻辑共同导致的。

---

## 最近重点报错与排障记录

下面这部分我专门按“现象 - 原因 - 解决思路”的方式整理，后面你复盘会比较方便。

### 1. 422 错误，请求体验证不通过

这个错误你这段时间遇到过很多次，本质都和“前端传过去的 JSON 结构，和后端 `BaseModel` 期望的不一致”有关。

常见原因有：

- 字段名写错，比如后端要 `session_id`，前端传成别的
- 类型不对，比如该传数字却传了空字符串
- 后端模型里声明了必填字段，但前端漏传

你后来逐步理解了这一点：FastAPI 不会“猜”你的意思，它只会严格按照 Pydantic 模型去校验。只要结构对不上，就直接 422。

### 2. 历史消息接口和会话列表接口发生路由冲突

这是一个非常典型的 FastAPI 路由问题。

你之前把消息详情写成 `/sessions/{session_id}/messages`，又把会话列表也写成了类似的动态路径，这样路由匹配就容易打架。结果前端请求某条接口时，看起来像命中了，实际上进的是另一个处理函数。

后面把历史会话列表改成 `/sessions/list/{user_id}/{role_name}` 之后，这个问题才真正稳定下来。

这个坑很有代表性，因为它说明：后端接口设计不只是“能访问”，还要考虑路径语义是否会互相覆盖。

### 3. 新建会话后前端没反应，`session_info` 不更新

这个问题不是后端不能创建，而是前端在拿到新 `session_id` 以后，没有把当前状态链完整更新。

也就是说，新会话创建成功只是第一步；前端还要同步更新当前 `sessionId`、清空旧消息、必要时重新刷新历史列表。否则接口虽然成功了，但用户看起来像“没有任何变化”。

这类问题很典型，属于“接口成功了，但页面状态没接上”。

### 4. AI 回复后端有，前端不显示

这个问题你也踩过。它看起来像模型没返回，实际上并不一定是模型问题，更多时候是：

- 前端没正确拿到 `reply`
- 消息列表没有刷新
- 或者又被路由冲突影响到了加载逻辑

后来你修正会话列表接口、消息加载顺序和前端状态更新之后，这块才真正通。

### 5. MySQL 连接失败或中途中断

这个问题出现时，往往不是 ORM 写错，而是数据库服务本身没正常运行，或者连接配置不对。

你这边后来就遇到了类似“服务没起来”或者连接异常的情况。这个时候正确思路不是先怀疑业务代码，而是先确认：

- MySQL 服务是否启动
- 端口是否正确
- 用户名密码是否对
- 数据库名是否存在

这是很重要的排障顺序：先排环境，再排代码。

### 6. Whisper 模型下载失败，出现 `LocalEntryNotFoundError` 或网络中断

这个是本地语音识别最常见的第一类坑。

报错像 `WinError 10054`、`LocalEntryNotFoundError` 时，通常不是你写的识别函数错了，而是 Hugging Face 模型在下载过程中被网络中断了。它本质上是依赖下载问题，不是识别逻辑问题。

正确思路是：

- 重试下载
- 保证网络稳定
- 模型下载成功后，后续就主要走本地缓存

### 7. 模型过于“伪人”，回答总是长篇大论

这不是 Python 报错，但它是非常真实的产品级错误。

问题本质不在于“模型笨”，而在于 Prompt 给了它错误信号：太强调角色腔调，太鼓励分析，太少限制普通聊天场景的长度和自然性。

你已经开始通过修改角色 Prompt 和意图判断规则修这个问题，这是对的。因为这种问题不该靠一堆硬编码兜底，而应该先从提示词和模式设计上解决。

### 8. 模型老是被旧记忆带跑，抓不住当前这句话的重点

这也是你今天提出来的非常关键的问题。

根因通常有三个：

- 当前输入太短，但旧上下文太强
- 长时记忆放得过重，盖过了当前消息
- 模式判断一旦进入训练，就容易延续分析语气

你目前已经做出的修正方向是正确的：

- 长时记忆只在 `training` / `review` 里参与
- 普通 `chat` 默认走更轻的上下文
- Prompt 明确要求先理解当前这句话的社交意图

这不是彻底解决，但方向是对的。

### 9. OpenRouter 模型 404，提示 `model_not_found`

这个报错不是你的聊天代码逻辑错了，而是你请求的模型名本身不存在、没权限，或者和当前兼容接口不匹配。

这种错误要优先检查：

- `model` 名称有没有写错
- 当前平台是否真的支持这个模型
- `base_url` 是否对应正确平台

后来你切回 DashScope 兼容模型后，请求能通，说明核心聊天链路本身没坏，问题在模型配置层。

---

## 当前还存在的待优化点

现在系统已经能跑，但离“稳定好用”还有几个很明显的提升空间。

第一，语音输入虽然已经通了，但识别速度还可以继续优化。现在 `faster-whisper` 用的是 `small` 模型，CPU 跑起来会偏慢。后面可以评估是否切到 `base` 或 `tiny`，或者至少在识别参数上先做速度优化。

第二，聊天体验已经比之前自然很多，但还没有完全稳定。尤其是“当前消息”和“旧记忆”之间的权重平衡，还需要继续调。后面你很可能还要继续修 Prompt、修模式判断策略、修记忆注入方式。

第三，历史会话虽然能用了，但还没有做会话标题生成、会话重命名、删除会话等更完整的产品能力。这个不是眼下最急，但后面一定会碰到。

第四，语音识别现在只是“语音转文字”。如果以后真要走陪玩方向，还可以继续往前做，比如自动聚焦输入框、回车发送、甚至后续做 TTS 播报。但这些都可以放在当前主链稳定以后。

第五，Electron 现在只是壳子。它已经能证明方向可行，但还没进入真正桌面应用阶段。像窗口置顶策略、托盘、透明背景、快捷键、打包发布，这些都还是后面的事情。

---

## 下次继续时最合理的衔接点

如果明天继续推进，我建议不要再回头纠结“能不能发消息”这种已经跑通的基础链路了，而是直接从下面几个方向选一个往前走。

第一条线，是继续修聊天体验。重点不是再堆功能，而是让模型别总被旧记忆带跑，让它更关注当前这句话。这条线会继续碰 Prompt、意图判断、记忆加载边界。

第二条线，是继续完善语音体验。现在已经能把语音识别回填到输入框，下一步最自然的优化就是让输入框自动聚焦、回车发送更顺手，慢慢把“边玩边说”的体验做起来。

第三条线，是继续稳住会话系统。虽然历史会话已经能看能点，但你还可以继续做“进入聊天页时默认定位最近会话”“会话标题更自然”“新建会话后自动刷新历史列表”这些稳定性优化。

第四条线，是在当前链路稳住后，再考虑更重的扩展，比如 RAG 深化、Neo4j 真正参与训练结构、或者训练流里的结构化训练档案。这些都能做，但最好建立在现有聊天主链已经足够稳的基础上。

---

## 阶段判断

到今天这一步，这个项目已经完成了一个很重要的跨越：它不再只是“能回复一句话”的智能体实验，而是开始拥有前端、后端、数据库、会话、工作流、语音和桌面化方向的完整雏形。

更重要的是，你的思维也在变。你现在问的问题，已经不再只是“这句代码怎么写”，而是“这个状态应该放前端还是后端”“这个接口的职责是不是混了”“这个记忆到底该不该参与当前回复”“这个功能在产品上到底有没有意义”。这说明你正在真正进入开发者视角。

这份文档后面还会继续补，因为这个项目现在还只是早期版本。但就今天为止，它已经是一个很扎实的阶段成果了。
